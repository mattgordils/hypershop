<header>
  <div class="px-margin">
    <{% if request.page_type == 'index' %}h1{% else %}div{% endif %} class="body header-logo">
      <a href="{{ routes.root_url }}" title="Go to homepage">
        {% render 'logo' %}
      </a>
    </{% if request.page_type == 'index' %}h1{% else %}div{% endif %}>
  </div>
</header>