<div class="px-margin">
  <div class="max-w-site-max-w mx-auto">
    <div class="grid grid-cols-4 2xl:grid-cols-6 gap-x-gutter gap-y-v-space-sm">
      {% for product in collection.products %}
        <div>
          {% render 'product_card' with product: product %}
        </div>
      {% endfor %}
    </div>
  </div>
</div>