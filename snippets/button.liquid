{% liquid
  assign button_class = 'button'
  if class != blank
    assign button_class = button_class | append: ' ' | append: class
  endif
%}

{% render 'link',
  label: label,
  target: target,
  class: button_class,
  to: to
%}